apiVersion: v1
kind: PersistentVolume
metadata:
  name: hold-hx-pv
spec:
  storageClassName: hx
  capacity:
    storage: 140Gi
  accessModes:
    - ReadWriteOnce
  flexVolume:
    driver: "hyperflex/hxvolume"
    fsType: "ext4"
    options:
      hyperflex.io/volumeid: "lunfile_12"
