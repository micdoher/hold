apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: hold-core
spec:
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  replicas: 1
  template:
    metadata:
      labels:
        app: hold-core
    spec:
      containers:
      - name: holdk8s
        image: micdoher/hold:bc08bf441590704baaae6bfb11986c2636dec9a1aa38483a56d87eb0b7ece5e7
        imagePullPolicy: "IfNotPresent"
        ports:
          - containerPort: 5000
      #
      # command: [ "/bin/bash", "-c", "--" ]
      # args: [ "while true; do sleep 30; done;" ]
